{
  "name": "ts-ping",
  "version": "1.0.0",
  "description": "A ping wrapper for mode with TypeScript",
  "main": "ping.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "author": "Govind S",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^13.13.5"
  }
}
