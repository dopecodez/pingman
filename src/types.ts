export type options = {
    continious?: Boolean,
    hostnameResolve?: Boolean,
    numberOfEchos?: Number,
    bufferSize?: Number,
    TTL?: Number,
    recordRouteHops?: Number,
    hopTimestamp?: Number,
    timeout?: Number,
    srcAddr?: String,
    numberOfAttempts?: Number,
    logToFile?: Boolean,
    logFilePath?: String
    IPV6?: boolean
}